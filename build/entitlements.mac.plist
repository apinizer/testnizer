<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow JIT for V8 / Monaco editor workers -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned memory execution (required by Electron + V8) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow loading of 3rd-party native modules (better-sqlite3 etc.) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Outgoing network (HTTP/SOAP/WS/GraphQL/gRPC requests) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- File read/write (local storage, import/export) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
</dict>
</plist>
